[
  {
    "label": "🏗️ Build firmware (app only)",
    "type": "shell",
    "command": "${SHELL}",
    "args": [
      "${COMMAND_FLAG}",
      "${EXPORT_WRAPPER} '${EXPORT_SCRIPT}'; idf.py build"
    ],
    "options": {
      "cwd": "${workspaceFolder}"
    },
    "group": {
      "kind": "build",
      "isDefault": true
    },
    "problemMatcher": []
  },
  {
    "label": "💣 Flash full (firmware + LittleFS)",
    "type": "shell",
    "command": "${SHELL}",
    "args": [
      "${COMMAND_FLAG}",
      "$env:LFS_BUILD='1'; ${EXPORT_WRAPPER} '${EXPORT_SCRIPT}'; idf.py -p ${PORT} flash"
    ],
    "options": {
      "cwd": "${workspaceFolder}"
    },
    "problemMatcher": []
  }
]
