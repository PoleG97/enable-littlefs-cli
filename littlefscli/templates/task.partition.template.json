{
  "label": "🔗 Flash LittleFS files - ${TAG}",
  "type": "shell",
  "command": "${SHELL}",
  "args": [
    "${COMMAND_FLAG}",
    "${EXPORT_WRAPPER} '${EXPORT_SCRIPT}'; idf.py -p ${PORT} ${PARTITION_LABEL}-flash"
  ],
  "options": {
    "cwd": "${workspaceFolder}"
  },
  "problemMatcher": []
}
